document.addEventListener('contextmenu', event => event.preventDefault());

const hamburgerImg = document.querySelector('.hamburger-icon-closed');
const hamburgerBtn = document.querySelector('.hamburger-menu');